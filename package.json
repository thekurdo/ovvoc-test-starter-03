{
  "name": "task-queue-lib",
  "version": "0.2.0",
  "description": "Simple in-memory task queue with concurrency control",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --verbose",
    "lint": "eslint src/"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "28.1.2",
    "eslint": "8.50.0"
  }
}
